name: CI

on:
  push:
    branches: [main]
    tags:
      - '*'
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installer uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Installer les dépendances
        run: uv sync --locked --all-extras --dev

      - name: Lint (ruff check)
        run: uv run ruff check .

      - name: Format check (ruff format)
        run: uv run ruff format --check .

      - name: Type check (mypy)
        run: uv run mypy .

      - name: Pre-commit hooks
        run: uv run pre-commit run --all-files --show-diff-on-failure

  tests:
    name: Tests
    runs-on: ubuntu-latest
    needs: quality
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installer uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Installer Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Installer SWI-Prolog (requis par pyswip)
        run: |
          sudo apt-get update
          sudo apt-get install -y swi-prolog

      - name: Installer les dépendances
        run: uv sync --locked --all-extras --dev

      - name: Lancer les tests avec couverture
        run: uv run pytest --cov --cov-report=xml --cov-report=term --cov-report=json

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: matrix.python-version == '3.12'
        with:
          name: coverage-report
          path: coverage.xml

      - name: Update Coverage Badge
        if: matrix.python-version == '3.12' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        uses: we-cli/coverage-badge-action@main

  build:
    name: Build exécutable
    runs-on: ${{ matrix.os }}
    needs: tests
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: linkedin-gameinator-windows.exe
          - os: ubuntu-latest
            artifact_name: linkedin-gameinator-linux
          - os: macos-latest
            artifact_name: linkedin-gameinator-macos

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installer uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Installer SWI-Prolog (requis par pyswip) - Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y swi-prolog

      - name: Installer SWI-Prolog (requis par pyswip) - macOS
        if: matrix.os == 'macos-latest'
        run: brew install swi-prolog

      - name: Installer SWI-Prolog (requis par pyswip) - Windows
        if: matrix.os == 'windows-latest'
        run: choco install swi-prolog -y

      - name: Installer les dépendances
        run: uv sync --locked --dev

      - name: Build de l'exécutable
        run: uv run pyinstaller --onefile --name ${{ matrix.artifact_name }} --paths src --hidden-import selenium.webdriver.firefox.webdriver "src/linkedin_gameinator/__main__.py"

      - name: Upload de l'artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}*

  release:
    name: Publier la release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Télécharger tous les artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist-artifacts

      - name: Publier sur GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          files: dist-artifacts/**/*
